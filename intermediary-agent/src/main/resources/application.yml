spring:
  cloud:
    openfeign:
      client:
        config:
          detection-client:
            logger-level: full
          metric-client:
            logger-level: full
redis:
  host: localhost
  port: 6379

logging:
  level:
    br.com.intermediary.intermediaryagent.gateway.core.DetectionClient: DEBUG
    br.com.intermediary.intermediaryagent.gateway.core.MetricClient: DEBUG


cloud:
  aws:
    credentials:
      instance-profile: true

services:
  metrics: localhost
  detection: localhost


s3:
  project-bucket: projects
  refactored-projects-bucket: refactored-projects

sqs:
  detect-pattern: detect-pattern