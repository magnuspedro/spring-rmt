<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.0.4</version>
        <relativePath/>
    </parent>

    <groupId>br.com.magnus</groupId>
    <artifactId>spring-rmt</artifactId>
    <version>1.1.0</version>
    <packaging>pom</packaging>
    <name>Spring RMT</name>
    <description>Automated Refactoring and Metrics Tool for Java projects</description>

    <modules>
        <module>config-starter</module>
        <module>project-sync-bff</module>
        <module>detection-and-refactoring</module>
        <module>metrics-calculator</module>
    </modules>

    <properties>
        <java.version>21</java.version>
        <!-- Override Lombok to a version compatible with Java 25 -->
        <lombok.version>1.18.38</lombok.version>
        <!-- Spring Cloud -->
        <spring.cloud-version>2025.1.1</spring.cloud-version>
        <spring-cloud-aws-dependencies.version>4.0.0</spring-cloud-aws-dependencies.version>
        <!-- Shared libraries -->
        <javaparser-core.version>3.26.1</javaparser-core.version>
        <rqueue-spring-boot-starter.version>4.0.0-SNAP-RELEASE</rqueue-spring-boot-starter.version>
        <ck.version>0.7.0</ck.version>
        <console-tree-builder.version>2.0</console-tree-builder.version>
        <!-- UI WebJars -->
        <bootstrap.version>5.3.3</bootstrap.version>
        <htmx.org.version>1.9.12</htmx.org.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <!-- Spring Cloud BOM -->
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>${spring.cloud-version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <!-- Spring Cloud AWS BOM -->
            <dependency>
                <groupId>io.awspring.cloud</groupId>
                <artifactId>spring-cloud-aws-dependencies</artifactId>
                <version>${spring-cloud-aws-dependencies.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <!-- Internal shared library -->
            <dependency>
                <groupId>br.com.magnus</groupId>
                <artifactId>config-starter</artifactId>
                <version>${project.version}</version>
            </dependency>
            <!-- JavaParser -->
            <dependency>
                <groupId>com.github.javaparser</groupId>
                <artifactId>javaparser-core</artifactId>
                <version>${javaparser-core.version}</version>
            </dependency>
            <!-- Rqueue -->
            <dependency>
                <groupId>com.github.sonus21</groupId>
                <artifactId>rqueue-spring-boot-starter</artifactId>
                <version>${rqueue-spring-boot-starter.version}</version>
            </dependency>
            <!-- CK metrics -->
            <dependency>
                <groupId>com.github.mauricioaniche</groupId>
                <artifactId>ck</artifactId>
                <version>${ck.version}</version>
            </dependency>
            <!-- Console tree builder -->
            <dependency>
                <groupId>io.bretty</groupId>
                <artifactId>console-tree-builder</artifactId>
                <version>${console-tree-builder.version}</version>
            </dependency>
            <!-- UI -->
            <dependency>
                <groupId>org.webjars</groupId>
                <artifactId>bootstrap</artifactId>
                <version>${bootstrap.version}</version>
            </dependency>
            <dependency>
                <groupId>org.webjars.npm</groupId>
                <artifactId>htmx.org</artifactId>
                <version>${htmx.org.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <configuration>
                        <annotationProcessorPaths>
                            <path>
                                <groupId>org.projectlombok</groupId>
                                <artifactId>lombok</artifactId>
                                <version>${lombok.version}</version>
                            </path>
                        </annotationProcessorPaths>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <configuration>
                        <argLine>
                            --add-opens java.base/java.lang=ALL-UNNAMED
                            --add-opens java.base/java.util=ALL-UNNAMED
                            --add-opens java.base/java.lang.reflect=ALL-UNNAMED
                        </argLine>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>
</project>
